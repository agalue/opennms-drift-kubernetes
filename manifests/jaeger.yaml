# @author Alejandro Galue <agalue@opennms.org>
#
# This is an optional component
# The following deploys an all-in-one instance for demo purposes.

---
apiVersion: jaegertracing.io/v1
kind: Jaeger
metadata:
  name: onms-tracing # Agent will be available through onms-tracing-agent
  namespace: opennms
spec:
  ingress:
    enabled: false
