# @author Alejandro Galue <agalue@opennms.org>

---
apiVersion: v1
kind: Service
metadata:
  name: cassandra-reaper
  namespace: opennms
spec:

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cassandra-reaper
  namespace: opennms
spec:
  replicas: 0