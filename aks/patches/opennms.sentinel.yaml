# @author Alejandro Galue <agalue@opennms.org>

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sentinel
spec:
  template:
    spec:
      affinity:
        $patch: delete
      containers:
      - name: sentinel
        resources:
          limits:
            memory: 1Gi
          requests:
            memory: 512Mi
          $patch: replace
